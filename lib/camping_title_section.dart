import 'package:flutter/material.dart';

class CampingTitle extends StatelessWidget {
  const CampingTitle({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
